# Claude Code
.claude/settings.local.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# DuckDB
*.duckdb
*.duckdb.wal

# Secrets and credentials — NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key
*-credentials.json
*-key.json
service-account*.json

# GCP
application_default_credentials.json

# Raw audit log data (real logs with project IDs, never commit)
data/raw_inspection/
data/real/

# Attack injection results (may contain project IDs, never commit)
data/attack_results/

# DB backups
*.duckdb.pre-attack-backup

# Frontend (src/report/frontend/)
node_modules/
dist/
.vite/
